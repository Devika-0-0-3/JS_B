var h= require("./sample");
var uc=require("upper-case");

//h.hello();
console.log(uc.upperCase(h.hello()));
h.hey();

//if we are integrating npm modules we should first use npm init
//then we can use npm install <module_name>
//then we can use the module

